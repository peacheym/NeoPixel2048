#ifndef FUNCTIONS
  #define FUNCTIONS

  //Function prototypes

  void up();
  void down();
  void left();
  void right();

  // HELPER FUNCTION TO RETURN RANDOM RGB VALUE
  void randomColor(){
    int r = random(151);
    int g = random(151);
    int b = random(151);
    //TODO: Return the associated HEX value.
  }
  
#endif
